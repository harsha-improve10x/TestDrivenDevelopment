package LargestNumberInAnArray;

public class LargestNumber {
    public int findLargestNumber(int[] arr) {
        int largest = 0;
        if (arr.length > 0) {
            largest = arr[0];
        }
        return largest;
    }
}
