package Easy;

public class MoveZerosEnd {
    public int[] move(int[] arr) {
        int[] result = {};
        if (arr == null || arr.length == 0) {
            return  result;
        } else if (arr.length == 1) {
            return arr;
        }
        return result;
    }
}
